# P4 R1–R3 整改交付

日期：2026-09-05。仓库 `D:/dev/canting`；P3 基线仍为 `ef1613c5f47fe089be6afbcd4ba491bccfbd0e82`。依据《餐盘_P4_产品复评_2026-09-05.md》整改 Prompt。

**R1–R3 修正与自动化验证完成，交回产品复评。P4 整体仍为部分完成；不自行宣布复评通过。** 未 commit/push、新建任务/工作树、进入P5或发布。

## 修正内容

- **R1 商家身份与解析不确定性分别保持。** OcrPipeline 的待确认行写入 merchant 品牌、brandOrigin=merchant、merchantContext。LocalFoodMatcher 在商家变化时只更新该行商家上下文，仍保留 parser_uncertain/manual、原名、数量、糖型杯型，不因切换商家自动解除解析待确认。显式单品品牌继续按P2保护。没有新增schema、补写旧legacy快照或修改历史。
- **R2 替换完整重置。** 新 requestId 到来时清空备注，按当前时间和既有独立 initialDate 入口上下文重新建立餐次/日期/时间，不继承旧的手动值。取消替换不动旧稿。日期/时间弹层返回前检查requestId，旧弹层结果不再写入新订单。
- **R3 评分统一真值。** quantity_error_indices共用逐项quantity-labels-v2，只有null标签被排除；高置信错误是身份、语义和明确数量错误集合的并集，同一项多错只计一次。dangerous_auto_accept从同一错误集合派生。新增自测直接提取并执行生产评分表达式，避免另一套测试公式掩盖错误。
- README已区分“最初只有文字”与“当前已有1张合成图、真实图0”，没有将文案更新当作补齐真实数据。

## 验证结果

| 验证 | 结果 | 原始证据（repair-20260905目录） |
|---|---|---|
| 复评原反例修复前 | 原8项通过，R1/R2两项失败 | before-probe.log |
| 数量故障注入修复前 | 5项中×100→1、×2→1两项失败 | before-metrics.log |
| 原复评及新增生产页/SQLite探针 | 19/19通过 | verified-probe.log |
| 相邻P2品牌/持久化/事务及P4竞态 | 45/45通过 | final-related.log |
| 评分器故障注入 | 5/5通过：×100→1、×2→1、无明确数量、同项多错、正确数量 | verified-metrics.log |
| 完整Flutter回归，concurrency=1 | 495/495通过 | final-full.log |
| 静态检查 | No issues found | verified-analyze.log |
| 变更Dart格式与git空白检查 | 通过 | format-final.log、diff-check.json |

19项探针包括OcrPipeline→真实确认面板→实际SQLite查库：商家甲、甲改乙、清空、明确单品品牌、未知不写记忆；数量原文×100及本次无糖/小杯保持。替换确认后新商家/商品/空备注/默认餐次时间正确落库；取消则旧备注与旧餐次日期时间完整保留；日期与时间旧弹层迟到分别验证。

完整回归后只做开发测试副本的日志/风格整理，未变断言；整理后的19项探针另行复跑通过，静态检查无问题。原审核工作区反例和失败证据没有修改。新增晚到日期测试最初错误使用页面范围外2001年导致断言，改为允许范围内2021年后通过，首次日志仍保留。

## 评分版本对照

同一冻结文字输入，基线和修复后均用v2错误集合重新计算，见 `scoring-v2-comparison.json`、`metrics.json`、`case-results.json`、`failures.json`。实际生产修复后仍为：明确数量5/6，高置信错误0/3，危险误判0/7。原×100目前是待确认，不因评分器反例被改写为生产已发生危险自动接受。

保留原分母规则、零分母null/not_measured、全部修复集且无独立留出声明。本轮没有更改任何标注来追成绩。原生图像指标属于上轮合成图执行，仍保存在run-20260905；本轮文本重算不冒充原生重跑。

## 证据与范围

新证据目录：`dev-docs/p4-evidence/repair-20260905/`。原132项清单在开工时全部匹配，快照为previous-changes.json；原验收报告保留为previous-report.md。run-20260905及P2/P3冻结材料未覆盖；相邻测试采集路径已切换新目录。最终p4-changes.json仍相对P3列全部交付文件，validation.json另外列出相对132项版本的整改路径和旧证据保护核验。

本轮仅Dart、测试、Python评分及文档变化，Android原生文件与上轮哈希一致，因此没有启动模拟器、Gradle或追加原生验证；上轮原生结果仍是历史证据。采用单进程串行Flutter回归，未因电脑此前重启而省略全量检查。

数据库仍v4，健康规则、推荐资格、奖励凭证与隐私清理边界未改。回退只需按整改路径恢复上轮对应文件，保留数据库、旧快照和原始证据；不要回滚用户后续改动。

## 仍待产品复评与材料补齐

真实平台截图为0，无独立留出、单人标注；S01与真实布局、完整危险场景数据集、数据集化跨重启指标仍缺。真机系统分享/相册/拍照、飞行模式、强杀/无GMS保持not_run，清单沿用p4-device-checklist.md。此次R1–R3通过自动化不代表这些项目完成。P5仍未启动。

请产品复跑原R1/R2反例及新版评分故障注入，核对真实页面/SQLite和最终哈希后裁定。当前没有新增政策取舍需要Owner决定。
