# 夜间双线开发指令（Phase 2 + Phase 3 · 无人值守方案）

> 生成时间：2026-09-04
> 用法：在 TraeCode 里手动开 2 个对话，分别粘贴「对话A指令」和「对话B指令」，然后去睡觉
> 前置状态：Phase 1 已完成（模块 1\~4），152 个测试全过，flutter analyze 零问题

***

## 一、方案总览

**为什么是两条线**：TraeCode 一个对话是串行的，开两个对话并行能把一晚上的产能翻倍。代价是两条线共用同一份磁盘代码，可能撞文件——所以两条指令里已经做了严格的文件权限划分和共享文件协议，AI 会自己遵守。

| <br /> | **对话 A（核心记录与展示线）**                                                                          | **对话 B（独立功能线）**                                   |
| ------ | ------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| 任务顺序   | 模块6 餐食记录 → 模块8 推荐详情与外卖跳转 → 模块15 手动添加餐食 → 模块5 首页                                             | 模块13 本地通知 → 模块10 个人设置 → 模块7 电子宠物 → 模块9 历史记录       |
| 预估工作量  | 约 11h                                                                                       | 约 9h                                              |
| 主战场    | `lib/ui/home`、`lib/ui/record`、`lib/ui/recommendation`、`lib/ui/manual_add`、`meal_repository` | `lib/ui/settings`、`lib/ui/history`、`lib/pet`、通知服务 |
| 排序逻辑   | 首页放最后，因为它要接线前面三个模块                                                                          | 通知放最前，因为设置页要接它的开关                                 |

**两条线的交叉点已经错开**：

- B线的宠物（第3个任务）和历史（第4个任务）需要按日期查餐食数据 → A线的第1个任务就是补 MealRepository 的日期查询（预计前2小时完成），时间上来得及；万一没完成，B线指令允许它自己新增只读查询方法兜底

- A线的首页（最后）只链接 A线自己的三个页面，不依赖 B线

- 共享文件（`app_state.dart` / `main.dart` / `pubspec.yaml` / `core_engine.dart`）两条线都可能碰：指令规定「改前重读 + 只做增量修改 + 绝不删对方代码」

**刻意留到今晚之后的**（避免夜间风险）：

- 外卖平台设置的 UI 和偏好持久化（A线只做跳转服务 + 默认配置，`shared_preferences` 依赖留给白天加）

- Phase 4 识别系统（模块11/12/14，OCR 是硬骨头，单独一晚）

- 模块16 全链路联调

***

## 二、睡前操作（5 分钟）

1. **（强烈建议）先提交 Phase 1 基线**：在任一 TraeCode 对话里发一句：

   > 把当前所有改动用 git 提交（git add -A && git commit，不要 push），message：V1.0 Phase 1 基线：持久化+食物库+指南接入+设置流程

   这样即使夜间翻车，Phase 1 的成果也丢不了。（tools/ 目录已加入 .gitignore，PDF 大文件不会被提交）
2. **新建对话 A**：粘贴下方【对话A指令】全文，发送
3. **新建对话 B**：粘贴下方【对话B指令】全文，发送
4. **确认两个对话的 AI 都已开始动手**（能看到它在读文件、写文件）再离开
5. **电脑保持接通电源**（已验证：当前电源计划在接通电源时永不睡眠，屏保无锁定）

**重要**：两个对话都切到智能体/自动模式，并开启「自动应用编辑」。否则 AI 每次改文件都会停下等你点确认，一夜就废了。

***

## 三、对话A指令（复制下面全文）

```
【夜间自动开发任务】「餐盘」APP V1.0 · A线：餐食记录与首页展示

项目目录：d:\dev\canting（Flutter + Provider + sqflite + go_router，仅 Android 验证）
背景：另一个并行对话（B线）同时在开发通知/设置/宠物/历史模块，你们共用同一份磁盘代码，必须严格遵守文末的文件权限规则。
要求：按顺序完成 4 个任务，全程不要向我提问、不要等待确认；拿不准的决策自行选合理默认，在最终总结里说明。

## 已完成的基础（不要重做）
- 持久化：user_profiles / meal_records / pet_states / user_custom_dishes / categories / dishes 表，UserRepository / MealRepository / PetRepository / CustomDishRepository（lib/data/）
- 匹配：DishMatcher（自定义菜品优先、包含匹配0.8、模糊≥0.7）、ServingEstimator（克重↔份数双向换算）、DietaryGuidelines（assets/data/dietary_guidelines.json 已注册加载）
- 计算：IntakeCalculator（Mifflin-St Jeor + 活动系数 → 1600~2400 五档能量插值）、RecommendationEngine（缺口排序/餐次时间推荐/菜品筛选，逻辑已完整，不是 Mock）
- 设置流程：6 步 onboarding 已落库；宠物对话数据 assets/data/pet_dialogues.json 已存在
- 当前 152 个单元测试全过，flutter analyze 零问题
- pubspec 已有：provider、sqflite、url_launcher、go_router。你今晚不许加任何新依赖

## 任务1：模块6 餐食记录（先做，B线依赖它的日期查询）
文档：dev-docs/module-06-meal-record.md（细节以文档为准）
先读文档 + lib/ui/record/ + lib/data/meal_repository.dart 现状，判断哪些已完成，只补缺失部分：
- MealRepository：按单日查询、按日期范围查询、增删改补全
- 分量系数（小0.8 / 常规1.0 / 大1.3）在记录与展示两端一致应用
- 删除记录：当日结构回退 + 宠物活力值回退（成长值不回退）
- 边界：跨日期添加、同餐重复添加、删除当日最后一条记录
- 新增单元测试（独立新文件，不改 test/widget_test.dart）

## 任务2：模块8 推荐详情页与外卖跳转
文档：dev-docs/module-08-recommendation.md
- 新建 lib/ui/recommendation/recommendation_detail_page.dart：推荐时间 + 餐次类型 + 推荐理由 + 3~5 道菜（主推+备选，来自 RecommendationEngine 结果），支持「换一批」
- 新建 lib/services/delivery_jump_service.dart：美团外卖 / 美团 / 饿了么 / 京东外卖 四平台；用 url_launcher 的 canLaunch 判断，已安装走 URL scheme 拉起，失败或未安装回落 H5 链接。先搜项目里现有的跳转代码并复用其思路——MVP 时期美团跳转失效是已知 Bug，重点修复
- 平台配置：本次用默认配置（全部启用、固定优先级），存储做成可注入的接口（方法签名留好），持久化（SharedPreferences）和设置页 UI 明确记录为遗留项，今晚不做
- 跳转目标 URL 的构造逻辑写单元测试

## 任务3：模块15 手动添加餐食
文档：dev-docs/module-15-manual-add.md
- 新建 lib/ui/manual_add/manual_add_page.dart：
  - 简单模式：选餐次 → 搜菜名 → 选分量（小/常规/大）→ 添加
  - 详细模式：输入克重 ↔ 份数双向联动（用 ServingEstimator）、选分类、标记「自制」、备注
- 搜索：自定义菜（CustomDishRepository + DishMatcher）优先，标准库在后；按使用次数排序；新菜名自动存入 user_custom_dishes 并累计使用次数
- 本地偏好：记住用户对某道菜调整过的分量，下次默认带出
- 添加后经 MealRepository 落库并刷新 AppState 当日数据
- 单元测试：搜索排序、克重份数联动、落库

## 任务4：模块5 首页（最后做，负责接线前三个任务）
文档：dev-docs/module-05-home.md
先读 lib/ui/home/ 现状，去掉一切 Mock：
- 环形完成度图 + 分类进度条：真实摄入 vs 目标（IntakeCalculator 结果）
- 今日餐食列表：来自 MealRepository 当日查询
- 推荐卡片：RecommendationEngine 结果，点击进推荐详情页（任务2）
- 底部「+」按钮：弹出选择 手动添加（任务3）/ 截图识别（入口占位，Phase 4 实现）
- 宠物区：保持现有 UI 与接口，只确认绑定真实数据；宠物引擎的深度改造归 B线，不要动 lib/pet/ 和 pet_repository.dart
- 空状态处理、当日数据变化实时刷新（Provider 侦听）
- Widget 测试放独立新文件

## 文件权限规则（B线并行开发中，严格遵守）
- 你独占：lib/ui/home/**、lib/ui/record/**、lib/ui/recommendation/**、lib/ui/manual_add/**、lib/services/delivery_jump_service.dart、lib/data/meal_repository.dart、lib/data/custom_dish_repository.dart、docs/night-progress-line-a.md
- 禁止修改：lib/ui/settings/**、lib/ui/history/**、lib/ui/about/**、lib/pet/**、lib/data/pet_repository.dart、lib/services/notification_service.dart、lib/ui/onboarding/**、lib/core/**（只读使用）、assets/data/**（数据基准不可动）、pubspec.yaml、test/widget_test.dart
- 共享文件（lib/state/app_state.dart、lib/main.dart、lib/core_engine.dart）：每次编辑前先重新读磁盘最新版（B线可能刚改过）；只做增量修改（新增字段/方法/导出），绝不整文件重写、绝不删除别人的代码；若对方的半成品导致编译错误，不要修也不要回滚，记录到进度文件后继续自己的任务
- meal_repository.dart 归你，但 B线可能往里新增只读查询方法：发现不认识的新方法请保留

## 质量与节奏
1. 每个任务完成后：flutter analyze → flutter test（全量）。自己的错误修完才进下一任务；若失败全部来自 B线负责的文件，记录后跳过该门槛继续
2. flutter 命令提示 waiting for another flutter command 是正常的（B线在跑），等它结束，不要杀进程
3. 旧测试禁止删除、跳过或弱化断言
4. 同一问题连修 3 次仍失败：该任务剩余部分跳过，写入进度文件，继续下一任务；绝不删除已完成的代码
5. 禁止：一切 git 命令、flutter run、flutter emulators、创建本任务之外的文档文件
6. UI 文案用中文；代码风格与现有项目一致；状态管理用 Provider
7. 每完成一个任务，更新 docs/night-progress-line-a.md：任务号 / 状态（完成或跳过）/ 改动文件清单 / 新增测试数 / 遗留问题

## 最终总结
4 个任务全部处理完后（无论完成或跳过）在对话里输出：每个任务做了什么、改动文件清单、新增测试数、遇到的问题及解决方式、遗留问题清单、最终 analyze/test 结果。
现在开始任务1。
```

***

## 四、对话B指令（复制下面全文）

```
【夜间自动开发任务】「餐盘」APP V1.0 · B线：通知 / 设置 / 宠物 / 历史

项目目录：d:\dev\canting（Flutter + Provider + sqflite + go_router，仅 Android 验证）
背景：另一个并行对话（A线）同时在开发餐食记录/推荐详情/手动添加/首页，你们共用同一份磁盘代码，必须严格遵守文末的文件权限规则。
要求：按顺序完成 4 个任务，全程不要向我提问、不要等待确认；拿不准的决策自行选合理默认，在最终总结里说明。

## 已完成的基础（不要重做）
- 持久化：user_profiles / meal_records / pet_states / user_custom_dishes / categories / dishes 表，UserRepository / MealRepository / PetRepository / CustomDishRepository（lib/data/）
- 匹配：DishMatcher（自定义菜品优先）、ServingEstimator（克重↔份数）、DietaryGuidelines（指南数据已加载）
- 计算：IntakeCalculator（Mifflin-St Jeor + 活动系数 → 1600~2400 五档能量插值，含饮食目标微调）
- 设置流程：6 步 onboarding 已落库；宠物对话数据 assets/data/pet_dialogues.json 已存在（先读它，在其上补充，不要推倒重来）
- 当前 152 个单元测试全过，flutter analyze 零问题
- pubspec 已有：provider、sqflite、url_launcher、go_router。你今晚只允许为通知功能加依赖（flutter_local_notifications 及其配套如 timezone），改 pubspec 前先重读磁盘最新版

## 任务1：模块13 本地通知
文档：dev-docs/module-13-notification.md
- 新建 lib/services/notification_service.dart：双端初始化、权限申请、展示通知、点击通知打开 APP
- 通知内容构造 API：「识别成功（标题+菜名+宠物台词）」和「识别失败」两类先做好，任意处调用即发；OCR 事件的真正触发留到 Phase 4，本次只做基础设施
- Android 侧完成 AndroidManifest 与通知渠道配置；iOS 侧把配置写好但在总结中注明未验证（Windows 无法构建 iOS）
- main.dart 初始化接入（main.dart 是共享文件，遵守共享协议）
- 可测部分（通知内容构造、开关状态）写单元测试

## 任务2：模块10 个人设置
文档：dev-docs/module-10-settings.md
先读 lib/ui/settings/ 现状，只补缺失部分：
- 个人信息编辑页：身高/体重/年龄/性别/活动量/饮食目标/作息，保存后用 IntakeCalculator 重算目标份数并更新档案（参考 onboarding 的落库逻辑）
- 宠物改名（PetRepository）
- 数据管理：清除全部数据（含二次确认弹窗；清除后回到 onboarding 首页）
- 通知开关：接任务1的 NotificationService
- 关于页：版本号、开源许可（cn-food-mcp 为 MIT 协议）、数据来源（《中国居民膳食指南(2022)》）
- 外卖平台设置 UI 不做（A线在做跳转服务，UI 是遗留项）
- Widget / 单元测试放独立新文件

## 任务3：模块7 电子宠物
文档：dev-docs/module-07-pet.md，先读 lib/pet/ 与 pet_repository.dart 现状，只补缺失部分：
- 活力值：基于最近 3 天饮食质量计算（需要按日期查餐食：优先用 MealRepository 已有的日期查询方法；若还没有，可在 meal_repository.dart 里「新增」带 query 前缀的只读方法，绝不修改已有方法）
- 成长值：累计只增不减；进化（幼年→成年）阈值与动画沿用现有实现
- 对话文案：按当日状态（达标/缺口类型/时段）生成，每种场景至少 3 条随机；基于现有 pet_dialogues.json 扩充
- 摸摸头互动：每日第一次互动加少量成长值（当日去重）
- 状态持久化到 pet_states（表已有）
- 不要改 lib/ui/home/**（A线在改首页）；如需首页配合改动，写进遗留问题
- 单元测试：活力值计算、成长值不回退、互动当日去重

## 任务4：模块9 历史记录
文档：dev-docs/module-09-history.md，先读 lib/ui/history/ 现状：
- 日历月视图：每日底色表示饮食质量（好/一般/差/无记录）
- 日详情页：当日结构 + 餐食列表 + 宠物状态；记录可查看/删除（删除行为与 meal_repository 现有删除逻辑保持一致，含活力值回退、成长值不回退）
- 周统计：平均完成度、食物种类数、坚果周进度、活力值趋势
- 查询方法缺失时同样只新增只读方法（query 前缀）
- 单元测试：质量评级规则、周统计口径

## 文件权限规则（A线并行开发中，严格遵守）
- 你独占：lib/ui/settings/**、lib/ui/history/**、lib/ui/about/**、lib/pet/**、lib/data/pet_repository.dart、lib/services/notification_service.dart、assets/data/pet_dialogues.json、docs/night-progress-line-b.md、pubspec.yaml（仅限添加通知相关依赖）
- 禁止修改：lib/ui/home/**、lib/ui/record/**、lib/ui/recommendation/**、lib/ui/manual_add/**、lib/data/custom_dish_repository.dart、lib/ui/onboarding/**、lib/core/**（只读）、assets/data/dishes.json、assets/data/categories.json、assets/data/dietary_guidelines.json、test/widget_test.dart
- meal_repository.dart 归 A线：你只允许「新增」只读查询方法（方法名加 query 前缀），不修改已有方法
- 共享文件（lib/state/app_state.dart、lib/main.dart、lib/core_engine.dart、pubspec.yaml）：每次编辑前先重读磁盘最新版（A线可能刚改过）；只做增量修改，绝不整文件重写、绝不删除别人的代码；对方半成品导致编译错误时不要修不要回滚，记录后继续
- 发现 app_state / meal_repository 里不认识的新字段、新方法：保留，那是 A线的成果

## 质量与节奏
1. 每个任务完成后：flutter analyze → flutter test（全量）。自己的错误修完才进下一任务；若失败全部来自 A线负责的文件，记录后跳过该门槛继续
2. flutter 命令提示 waiting for another flutter command 是正常的（A线在跑），等它结束，不要杀进程
3. 旧测试禁止删除、跳过或弱化断言
4. 同一问题连修 3 次仍失败：该任务剩余部分跳过，写入进度文件，继续下一任务；绝不删除已完成的代码
5. 禁止：一切 git 命令、flutter run、flutter emulators、创建本任务之外的文档文件
6. UI 文案用中文；代码风格与现有项目一致；状态管理用 Provider
7. 每完成一个任务，更新 docs/night-progress-line-b.md：任务号 / 状态（完成或跳过）/ 改动文件清单 / 新增测试数 / 遗留问题

## 最终总结
4 个任务全部处理完后（无论完成或跳过）在对话里输出：每个任务做了什么、改动文件清单、新增测试数、遇到的问题及解决方式、遗留问题清单、最终 analyze/test 结果。
现在开始任务1。
```

***

## 五、早上起来做什么（按顺序）

### 第 1 步：看战报

- 两个对话各自的最终总结（滚到对话底部）

- 项目里新增的 `docs/night-progress-line-a.md` 和 `docs/night-progress-line-b.md`

- 如果某条线中途停了（没有输出最终总结）：先对那个对话发「继续」，让它跑完，再做第 2 步

### 第 2 步：新开一个对话，粘贴【晨间收尾指令】

```
昨夜两条线并行开发了「餐盘」APP（A线：餐食记录/推荐详情/手动添加/首页；B线：通知/设置/宠物/历史）。请做收尾整合：
1. 读 docs/night-progress-line-a.md 和 docs/night-progress-line-b.md
2. flutter analyze：修掉所有问题，重点是两条线在 app_state.dart / main.dart / pubspec.yaml / core_engine.dart 里的集成冲突；两边代码都要保住，不能删任何一边
3. flutter test 全量：修掉所有失败；禁止删除、跳过或弱化测试
4. 检查两个进度文件里的「遗留问题」清单，能安全修的顺手修掉
5. 输出：修复清单 / 最终 analyze 与 test 结果 / 仍遗留的问题
全程不要提问，自行决策。
```

> 就算早上看到一片红也不要慌——两条线并行必然有集成毛边，这正是晨间指令的第一道工序。

### 第 3 步：人工冒烟测试（不懂代码也能做）

在模拟器上跑起来（TraeCode 终端里 `flutter run`），检查：

- [ ] 首页有真实数据（环形图、进度条、宠物）

- [ ] 手动添加一道菜（试试详细模式输克数，看份数自动算出来）

- [ ] 首页列表出现这道菜，推荐卡片能点进详情页

- [ ] 删除这条记录，首页数字回退

- [ ] 设置页改个体重保存，不报错

- [ ] 历史页能看到今天

- [ ] 杀掉 APP 重开，数据还在

- [ ] 有 Bug 就把现象描述给 TraeCode 让它修

### 第 4 步：全绿后提交

```
收尾和冒烟测试都通过了：git add -A && git commit -m "V1.0 Phase2-3：双线夜间开发（餐食记录/推荐跳转/手动添加/首页/通知/设置/宠物/历史）"。不要 push。
```

***

## 六、风险与应对

| 风险             | 应对                                              |
| -------------- | ----------------------------------------------- |
| 对话中途停下等确认      | 指令已要求全程不提问；若因 TraeCode 单次回复的回合上限停下，早上发「继续」即可续上  |
| 两条线改坏同一个文件     | 指令已规定共享文件「改前重读 + 只做增量 + 绝不删对方代码」；晨间收尾指令第一步就是修集成 |
| flutter 命令互相等锁 | 正常现象（显示 waiting for startup lock），会自动等待，不用管     |
| 某个模块反复失败       | 指令规定连修 3 次失败就跳过并记录，不会整夜卡死在一处                    |
| 电脑睡眠 / 锁屏      | 已验证电源计划（接电源永不睡眠）；睡前确保插着电源就行                     |
| 夜间成果质量差        | 有 152 个旧测试兜底 + 每模块新测试 + 晨间收尾指令，三层保险             |

***

## 七、今晚之后还剩什么

| 事项                            | 说明                                                |
| ----------------------------- | ------------------------------------------------- |
| 外卖平台设置 UI + 偏好持久化             | A线今晚只做跳转服务和默认配置，UI 遗留（需要加 shared\_preferences 依赖） |
| 模块12 Android 分享扩展 + PaddleOCR | Phase 4，原生代码 + 模型集成，单独安排                          |
| 模块14 APP 内拍照识别                | Phase 4                                           |
| 模块11 iOS 分享扩展 + Vision OCR    | 注意：Windows 上无法构建验证 iOS，建议最后单独处理或找 Mac 环境          |
| 模块16 全链路联调                    | Phase 5，识别系统完成后做                                  |

