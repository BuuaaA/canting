# 04：400 根因及限定修复（2026-09-15）

## 结论

部分完成，未合入、未启动07，未达到 `ai / ai_validated`。

本轮首次完成电脑和手机**相同 UTF-8 字节**的虚构请求对照。两端都复现 `400 / prompt too long`；去掉逐日重复明细后，两端都变为 `503 / provider unavailable`。原“客户端已排除”结论不成立：客户端确实发送了不必要的超长 prompt，现已最小修复。

## 根因与修改

`NextMealRequest.toJson()` 原样使用 `Rolling7dIntakeStats.toJson()`，会把七天每天十类统计再次全部展开到 `rolling7d.days`。这一字段不是下一餐请求所需的窗口汇总。

修复仅在远端请求序列化中删除 `rolling7d.days`。本地统计、今日字段、窗口汇总、unknown/null、revision、日期餐次、排除项、共享时间预算及正式默认关闭保持不变。

新增回归测试先在旧代码失败，再在修复后通过，确认本地七天明细没有被删除，远端仍保留汇总及未知语义。原587项已通过测试未重做。

修正错误摘要解析对下划线错误码的识别，安全输出 `prompt too long`、`provider unavailable`。

## 真实对照

全部请求均为 POST `cantingan-proxy-lutwihtgyl.cn-beijing.fcapp.run/recommend`，外层只有字符串类型的 `prompt`。口令使用用户此前授权的临时凭据，经关闭回显的运行时输入提供；未保存源码、文件或APK。

| 固定虚构输入 | 请求字节 | prompt字符 | 电脑 | 手机 |
|---|---:|---:|---|---|
| 七天逐日展开 | 21573 | 18638 | 400 / prompt too long | 400 / prompt too long |
| 同源数据，仅移除days | 3104 | 2677 | 503 / provider unavailable | 503 / provider unavailable |

每组两端 SHA-256 一致，详见工作区 `artifacts/android/20260915-paired-results.jsonl`（从真实CLI结果提取、规范化的脱敏记录，不是完整响应正文）。

手机超长请求ID：`1-6aa8e535-13b06a64-204996dd9261`。

手机摘要请求ID：`1-6aa8e535-13d68d3f-1cf6b27a4616`。

电脑摘要请求ID：`1-6aa8e51a-1315136b-cea697d24903`。

## 真机宿主和实际页面

系统仍拒绝 ADB 模拟输入（INJECT_EVENTS），因此新增 `tool/fc_app_diagnostic.dart`，仅在 debug 下通过 Flutter 调试通道接收内存凭据。它调用真实 `runCantingApp`，加载原SQLite数据及AppState，发送固定虚构对照后，再调用实际 `AppState.loadNextMealRecommendation(force: true)` 并通过现有 GoRouter 打开 `/recommendation`。

固定虚构对照由宿主内的 `dart:io HttpClient` 发送，不计作 AppState 业务链路通过证据。最后单独执行的实际页面结果是 `source=local_rule / reasonCode=timeout / suggestionCount=3 / pageOpened=true`，没有渲染AI菜品，因此没有通过AI验收。

宿主每个诊断请求/必要重试期间共享内存凭据，finally清空；电脑进程结束后亦不保留。该宿主不是正式入口，须显式指定target及开发开关。当前手机安装的是此自动验证debug包。

安装APK与构建产物SHA-256一致：`41146879b064137e410b2e4a1866114fe89337f3d1537a0fc0fe438e414ae22c`。

## 下一步阻塞

需要已部署FC中摘要请求ID对应的**上游错误日志**（上游HTTP状态、错误码、耗时；禁止Key/Authorization/饮食内容）。当前环境没有FC控制台或部署账户连接工具。`provider unavailable` 仅证明当前代理报告上游不可用，不能据此推定百炼Key错误、模板错误或具体故障组件。

不改服务端密钥或模板，不以本地候选代替AI成功。上游故障明确/恢复后可复用本次自动验证宿主继续页面验收，避免重复人工输入。临时口令本轮结束后需轮换。

## 文件与验证

- `lib/services/next_meal_recommendation.dart`：远端最小摘要修复。
- `test/services/next_meal_recommendation_test.dart`：逐日明细不上传回归。
- `lib/services/fc_next_meal_remote.dart`：安全错误说明识别；本轮之前诊断改动保留。
- `tool/fc_app_diagnostic.dart`：仅开发的真机自动验证宿主。
- `lib/main.dart` 为先前开发接线修改，保留；平台生成文件不纳入本次交接。

工作区证据根目录：`C:/Users/gxy20/Documents/Codex/2026-09-11/app-04/artifacts/android/`。静态检查通过、定向21项通过、debug构建及安装成功。没有执行合入或07。
