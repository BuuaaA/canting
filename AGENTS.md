# 餐盘 Canting：项目级长期指令

本文件是本仓库所有 Code Agent（Trae Code、CodeBuddy、Codex 或其他开发代理）的长期约束。开始任何任务前必须完整阅读。除非用户或产品经理明确要求，不得修改本文件。

## 1. 角色与决策权

- 用户是产品 Owner，负责最终取舍。
- Codex 担任产品经理，负责产品定义、优先级、验收标准和版本范围。
- Trae Code / CodeBuddy 担任开发团队，负责现状审计、技术方案、实现、测试和交付报告。
- 开发团队可以提出替代方案，但不能自行改变产品定位、健康规则含义、数据隐私边界或版本范围。
- 遇到会改变用户体验、数据含义或健康判断的歧义时，先完成安全的只读调查，再把“现状、选项、影响、推荐”提交产品经理确认。

## 2. 产品定位

餐盘是一款 Android 优先、离线优先的轻量化饮食干预 App，面向经常点外卖、想改善饮食结构但不愿精确计算热量的普通用户。

核心用户价值：

1. 用户主动分享外卖订单截图或从 App 内选择图片。
2. App 在本地 OCR，提取菜品名称并进行粗粒度语义归类。
3. 用户可以低成本确认或纠正。
4. App 根据最近 7 天和 28 天的总体饮食结构给出温和、可执行的下一餐建议。
5. 目标是让长期饮食结构逐步趋向《中国居民膳食指南（2022）》，不是追求单餐完美。

产品不是精确卡路里计算器、医疗诊断工具、外卖平台监控器或惩罚式节食工具。

## 3. V1 范围

V1 必须做到：

- Android 首发。
- 用户主动分享截图、相册选择或拍照后进行本地 OCR。
- 本地菜品匹配、明确的置信度和可编辑识别结果。
- 未识别商品可手动归类，并在本机记住用户确认结果。
- 本地餐食记录、历史、用户设置和宠物状态持久化。
- 基于确定性规则的 7 天与 28 天结构分析和下一餐推荐。
- 默认不主动推荐已知不健康或信息不足的高风险食品。
- 无网络时核心流程完整可用。

V1 明确不做：

- 文本或视觉大模型、联网搜索和在线商品识别。
- 社区、排行榜、跨用户学习和云端用户画像。
- 自动监控其他 App、后台截屏、读取外卖订单通知。
- iOS 发布。
- 精确克重、精确热量或医疗级营养结论。

允许为未来 AI、社区和同步能力设计可替换接口或可扩展字段，但不得引入 AI SDK、云服务、网络权限、空壳页面或未被当前需求使用的复杂架构。

## 4. “越用越聪明”的严格定义

“越用越聪明”只允许表示：

- 用户纠正过的品牌、原始商品名、标准化名称、类别、做法、糖型、杯型、份量档位等事实保存在本机。
- 相同或高度相似商品再次出现时，优先复用用户确认结果。
- 随使用增加，重复确认次数下降。
- 推荐依据用户近期记录、明确偏好和膳食结构变化进行个性化排序。
- 所有学习结果可查看、可编辑、可删除、可导出。

它绝不表示：

- 用户行为自动改写《中国居民膳食指南》规则。
- 因为用户常吃某食品，就把它重新定义为健康食品。
- 在本机训练不可解释、不可回退的模型。
- 未识别食品静默记成 0 份、忽略或当作健康食品。

## 5. 三层数据边界

### 5.1 GuidelinePolicy：稳定规则层

负责：

- 指南目标、食物类别贡献、7/28 天窗口规则。
- 主动推荐准入、限制、排除和风险暴露规则。
- 推荐原因与不推荐原因。
- 规则版本和数据库迁移。

要求：确定、可测试、可解释、可版本化。用户纠正和个人偏好不能直接修改这一层。

### 5.2 FoodKnowledge：内置知识层

负责：

- 标准食品类别、标准菜品、品牌商品别名。
- 常见配料、做法、糖型、杯型、份量区间和质量标签。
- 数据来源、估算标记、置信度、审核状态和数据版本。

原始采集数据不是可直接发布的数据。导入 App 前必须经过许可核查、去重、规范化、异常检测、质量分层、体积评估和迁移测试。

### 5.3 UserFoodProfile：用户本地知识层

负责保存：

- 品牌、原始名称、标准化名称和匹配键。
- 用户确认的类别、做法、糖型、杯型、份量档位和适用风险标签。
- 数据来源、确认时间、更新时间、使用次数和数据结构版本。

用户纠正只影响该用户本机。优先保存可验证事实，不要只保存“健康/不健康”结论。

## 6. 识别与纠错契约

- OCR 文本、商品解析、语义匹配和份量估算是不同阶段，不得混成一个不可测试函数。
- 高置信度：可以自动填入，但必须允许用户修改。
- 中置信度：展示候选并要求一键确认。
- 低置信度或未知：明确提示暂时无法识别，提供手动归类入口。
- 未识别项不得静默记为零份，不得自动进入健康推荐。
- 用户确认后写入 UserFoodProfile；下次优先命中本地确认结果。
- 置信度阈值必须集中配置、有测试依据，不能散落为魔法数字。
- 识别结果必须保留 source、confidence、matchedBy 和 policyVersion 等可解释信息。

## 7. 推荐与健康安全契约

推荐引擎必须先执行确定性的准入过滤，再进行个性化排序。

默认不主动推荐：

- 酒精。
- 含糖饮料和配方不明确的奶茶。
- 明确油炸食品。
- 明确高油、高盐、高糖食品。
- 信息不足且存在明显风险的食品。

不能只根据“汉堡”“三明治”等名称永久判定不健康。已确认由非油炸蛋白、主食和蔬菜组成，且无酱或少酱的版本，可以按事实参与条件推荐；做法和配料未知时保持保守，不主动推荐。

推荐输出必须带稳定的 reason code 和用户可读解释，例如：

- `vegetable_deficit_7d`：最近 7 天蔬菜不足。
- `sugary_drink_exposure_7d`：最近 7 天已记录含糖饮料。
- `unknown_preparation`：做法不明确，因此不主动推荐。
- `user_confirmed_light_version`：用户确认该商品为非油炸、少/无酱版本。

提醒必须温和，不阻止记录，不羞辱用户，不声称单次选择决定健康结果。

## 8. 估算原则

- 输出小/正常/大份，小/中/大杯，无糖/低糖/常规糖/高糖/未知，以及大致贡献区间。
- 数据不足时不得生成看似精确的克数、卡路里或营养素数值。
- `estimated`、来源和不确定性必须贯穿数据模型、界面和测试。
- App 的优化目标是长期结构趋近，不是让每一餐或每一天机械达标。

## 9. 隐私、离线和轻量化

- 核心链路必须离线可用，用户数据默认只存本机。
- 截图只为完成本次识别使用；除非产品需求另有说明，不长期保存原图。
- 不得添加遥测、广告、第三方云上传或联网权限，除非有单独 PRD 和用户授权。
- 不因“未来可能需要”引入重量级依赖。
- 包体优化必须先用构建分析证明来源，再处理 ABI、符号、资源、OCR 模型或依赖；不能凭猜测删除功能。
- 面向可能没有 Google Play 服务的国内 Android 设备时，不能未经真机验证就把内置 OCR 改成动态下载模型。

## 10. 工程工作流

每个任务必须按以下顺序执行：

1. 读取本文件、任务指定文档、相关代码和测试。
2. 执行 `git status --short --branch`，识别未提交改动和其他会话正在编辑的文件。
3. 先报告当前行为与目标行为的差距；若工作树存在重叠改动，不得覆盖或回滚。
4. 选择最小、可迁移、可测试的实现，不做无关重构。
5. 先补关键测试，再完成实现；涉及数据库时必须覆盖旧版本迁移和用户数据保留。
6. 运行格式化、静态检查、相关测试和完整回归测试。
7. 涉及 Android 原生能力时，除了单元测试，还必须给出真机验收步骤。
8. 未经明确指示，不执行 commit、push、reset、checkout 丢弃或删除用户文件。

最低质量门槛：

- `flutter analyze --no-pub` 无错误和警告。
- `flutter test --no-pub` 全部通过，不允许用“另一会话在途”作为最终豁免。
- 新增核心规则有正例、反例、边界和迁移测试。
- 未识别、空数据、旧数据库、权限失败和原生通道不可用都有降级路径。

## 11. 每次交付格式

开发团队完成任务后必须提交一份交接报告，包含：

1. 当前结论：完成、部分完成或阻塞。
2. 实际修改内容与文件清单。
3. 数据结构、规则或用户体验变化。
4. 执行过的命令和真实结果。
5. 未执行的验证及原因。
6. 已知风险、遗留问题和回滚方式。
7. 需要产品经理确认的事项。
8. 下一步建议，但不得擅自开始下一阶段。

## 12. 文档优先级

发生冲突时按以下顺序判断：

1. 用户在当前任务中的明确指令。
2. 本 `AGENTS.md`。
3. 产品经理最新批准的阶段任务书和验收标准。
4. 当前代码与自动化测试所证明的真实行为。
5. `dev-docs/` 中最新且注明日期的文档。
6. 旧 PRD、旧开发指南和 README。

旧文档可能描述 mock 数据、iOS 双端首发、PaddleOCR 或尚未接入 OCR 等已经过时的状态。不得仅凭旧文档回退现有实现；必须以当前代码、Git 和测试为准。
