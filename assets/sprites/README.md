# Pet sprite assets

Place 64 x 64 pixel PNG frames in this directory.

File names must follow:

`pet_{type}_{stage}_{state}_{frame}.png`

Example:

`pet_cat_baby_energetic_0.png`

Until final art is added, `PetSpriteWidget` renders a colored placeholder when
a requested frame is missing.
